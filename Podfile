# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'audio-recognizer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for audio-recognizer

	pod 'Firebase'
	pod 'Fabric'
	pod 'Crashlytics'
    pod 'Alamofire'

  target 'audio-recognizerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'audio-recognizerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
